# 更新说明

## 1.8.2
- 数据管理页新增加密备份导出（.bpx）：设置至少 8 位口令后以 AES-256-GCM 认证加密整个 Excel 备份；口令经 PBKDF2-HMAC-SHA256（31 万次迭代）派生，不落盘，遗失后无法恢复数据。
- 加密备份导入：选择 .bpx 文件时提示输入口令；口令错误或文件被篡改会明确提示并可重试；解密后的内容仍走原有全部 ZIP/XML/业务安全校验与预览流程。
- 明文 .xlsx 导出/导入保持不变并继续兼容 v2/v3 格式；明文导出前的风险警告保留。
- 历史日历新增备注红点：存在自定义备注的日期下方显示小红点，双击该日期自动跳转并定位到对应带备注的记录。
- 底部导航条左右端头改为精确弧形（胶囊）显示，修复部分设备上出现的方形边角。
- 修复发布流水线 build-release.yml 中两处步骤缩进丢失导致的 CI 解析失败。

## 1.8.1
- 提醒时间改为选择后自动保存并立即重排，删除「保存提醒设置」按钮。
- 「记一次血压」调整为日期时间 → 测量数据 → 测量场景 → 不适 → 影响因素 → 补充说明的填写顺序；保存按钮移至滚动页面最下方，原有可点击校验规则保持不变。
- 修复历史日历滚动后回到顶部时「历史记录」标题可能无法恢复的问题。
- 系统状态栏和导航栏使用透明 edge-to-edge，关闭 Android 10+ 三键导航强制对比层；浅色、深色模式自动匹配系统图标。
- Room 升级至 v7，逐条持久化 ALL / DISCARD_FIRST 平均策略；旧记录按已存收缩压、舒张压、脉搏三项平均值安全回填。
- Excel 备份升级至格式 v3 并继续读取 v2；导出直接使用保存时的派生值，不再用默认策略重算。
- Apache POI 升级至 5.5.1；旧表与已导入记录按 record_id 去重，避免同机往返产生重复导出。
- 药品与时间点写入改为事务和差量更新，未修改的时间点保留原 id 及既有服药打卡历史；日历同步失败时不再继续制造重复日程。
- 导入或清空数据后同步刷新血压提醒、服药提醒和桌面小部件；无测量记录时也允许导出用户资料与设置。
- 上架目标升级至 Android 15（API 35）；精确闹钟改用用户可授权权限，未授权时继续安全回退为非精确提醒。
- 清理未引用资源、重复启动图标和遗留声明；收窄 Apache POI 的 R8 保留范围，减小 Release 包体。
- 发布流水线同时产出并验签 APK/AAB，签名 Secret 任一缺失时立即失败，避免误发布 unsigned 包。

## 1.8.0
- 新增服药提醒模块：设置→提醒设置中可添加多种药品（名称、每次数量、每药多个每日服药时间），到点通过精确闹钟提醒（含开机/时区变化自愈重排；系统撤销精确闹钟权限时回退非精确闹钟）。
- 首页新增「今日服药」卡片：按时间列出每种药并打勾记录，勾选历史存入本地数据库（Room v6，新增 medications / medication_times / medication_intake_logs 三张表，无损迁移）。
- 今日概览卡片压缩为紧凑版：数据合并为一行，删去「看看本月日历」入口；有记录时保留「看看今天的记录」。
- 新增「同步到系统日历」开关（需日历读写权限）：自动创建每日重复的服药日程（含日历提醒），修改/删除药品或关闭开关时全量重建，不留孤儿日程。
- 新增桌面小部件（Jetpack Glance）：2×2 最新血压+分级；4×2 加今日测量次数与服药进度；4×4 再加近 7 天迷你双折线趋势图；保存记录/打卡后自动刷新，点击进入应用。
- 「清空全部数据」同步清除药品与服药勾选历史。
- 说明：本版本备份导出暂不包含服药数据，将在后续版本补充。

## 1.7.1
- 「记一次血压」和「编辑测量」中的血压/脉搏输入数字、平均值展示改为普通字体，不再使用粗体数字字体（Caprasimo 仅保留在首页大数字等展示场景）。
- 顶部返回按钮和页面标题改为滚动自动隐藏：向下浏览内容时顶栏逐渐收起，往回滚动时立即恢复显示，松手后自动吸附到全显/全隐。
- 滚动隐藏行为统一应用到：记一次血压、编辑测量、记录详情、血压记录（历史）、设置及全部设置子页（用户资料/提醒/显示/数据管理/应用说明/更新说明）。
- 首页与趋势页标题本就随内容滚动，无固定顶栏，保持原样。

## 1.7.0
- 全套「暖阳打卡」温暖化 UI 改版：奶油底 + 陶土橙/鼠尾草绿、药丸控件、口语化文案、连续打卡卡片与浮动底部导航。
- 转场重做：底部 Tab 平级切换改为轻量淡入淡出，子页进出用容器滑动 + Material 减速曲线；支持 Android 13+ 预测式返回手势。
- 启动时申请当前分辨率下最高刷新率显示模式，适配被厂商默认锁 60Hz 的高刷屏。
- 时间选择统一为 24 小时制轮盘（时/分双滚轮），修复新增测量日期按钮在大字号下文字显示不全的问题。
- 升级 Kotlin 2.0 / Compose 1.7（strong skipping）/ Navigation 2.8，切换与滚动性能整体提升。
- 血压分级统一采用《中国高血压防治指南》成人诊室血压标准（正常高值、1/2/3级高血压），数据库升级至 v5，历史记录的分级与高风险标记自动无损重算。
- 新增“血压偏低”提示（收缩压 <90 或舒张压 <60）；高风险提醒阈值改为含边界（≥180 和/或 ≥120），并与分级拆分为独立概念。
- 显示设置新增“平均值不计第一组读数”开关；保存、编辑、备份导出/导入和撤销删除全链路保持策略一致（导入时按备份平均值自动识别策略）。
- 修复首页今日概览、历史本周/本月、趋势 7/30 天窗口在跨零点后不刷新的问题；打开趋势页后新增的记录实时进入折线。
- 日历改为周一开头，与“本周”统计使用同一种周定义。
- 测量提醒改用精确闹钟（系统撤销权限时自动回退），应用启动时自愈重排；通知图标改为应用自有图标。
- Excel 导入增加解压炸弹防护（压缩比与单条目大小上限）；症状列非法时按分隔符拆分并计入自动修正，不再静默丢失。
- “清空全部数据”同步重置全部设置与上次导出时间；保存成功提示不再依赖文案字符串驱动页面跳转。
- 分级中文标签收敛为单一来源，清理遗留死代码、无用路由与归档源码。

## 1.6.2
- 历史页保留按月日历，并新增“近期”模式，可切换当前自然周和当前自然月。
- 本周固定以周一为首日；周、月边界均按设备本地时区计算并使用半开区间。
- 近期列表和统计复用 Room 轻量投影及 SQL 聚合，不加载原始读数或完整症状。
- 修复趋势图派生状态捕获旧 viewport 的问题，折线和节点统一使用同一渲染点集。
- 横轴刻度按图表宽度动态限制并过滤碰撞，首尾标签保持在图表边界内。
- 新增、编辑、晨间提醒和晚间提醒统一使用 24 小时数字时间输入。

## 1.6.1
- 日历月份查询只读取当前月的时间戳与风险标记，单日列表只读取展示所需投影，详情才加载原始读数。
- 首页改为最近记录投影与今日 SQL 聚合；趋势计数、均值、极值和高风险次数下推 Room。
- 月份和日期切换使用可取消 Flow，移除历史页重复数据库订阅，页面 Flow 改为生命周期感知收集。
- 缓存日历矩阵和日期语义，保留 LazyColumn 稳定 ID，减少无关 Compose 计算。
- Excel 导入取消整文件字节复制，使用限流输入流、250 条批量事务写入和导入中进度状态。
- 新增 Baseline Profile/Macrobenchmark 模块、范围查询计划测试、10,000 条压力测试和可重复 SQLite 查询基准。

## 1.6.0
- 历史记录改为按月日历，有记录日期可选择，无记录日期明确禁用。
- 点击日期查看当天全部记录，详情、编辑、删除和撤销会自动刷新日历。
- 新增和编辑改用日期、时间选择器、可删除读数组及固定保存操作区。
- 首页突出最近血压和今日状态，趋势增加文字摘要。
- 提醒时间使用 TimePicker，并增强深色模式、大字号、中文和 TalkBack 语义。

## 1.5.1
- 高风险判断统一为“任一组原始读数或平均值达到标准”，Room v4 迁移会无损修正旧记录的风险字段。
- Excel 导入以原始读数为唯一事实来源，新增格式、大小、数量、时间、读数范围、重复 ID 和事务完整性校验。
- 导入结果显示新增、覆盖、自动修正、跳过和错误数量；同一备份可幂等重复导入。
- 数据库测量时间继续使用 Unix epoch 毫秒，所有自然日/月查询统一为本地时区半开区间并覆盖夏令时边界。
- 新增和编辑表单使用 SavedStateHandle 恢复草稿，保存失败保留、成功或明确放弃时清除。
- 关闭 Android 系统自动备份；移除源码树中的本地密钥文件并禁止 Release 健康数据日志。
- 提醒增加通知授权状态、系统设置入口，以及系统时间/时区变化后的重新安排。

## 1.5.0
- 趋势图改为真实时间轴，7 天和 30 天显示每次测量，“全部”显示每日平均并支持当天明细。
- 数据库升级至 v3，增加测量时间索引和轻量趋势查询，迁移过程不丢失记录。
- Excel 备份升级至 v2，使用独立“原始读数”工作表保存任意组数，并补齐场景、症状。
- 新增 Excel 备份导入，支持同 ID 覆盖、其他记录保留及用户资料/显示设置恢复。
- 血压分级和高风险阈值统一为单一规则来源；高风险开关和确认保存时序修复。
- 新增真正的晨间/晚间系统通知，设备重启或应用升级后自动恢复。
- 大字号、趋势显示开关、事务清空和新增测量的时间/场景/症状输入正式生效。
- 清理重复数据库、旧页面、Mock 和 V2/V3/V4 命名；补充 LICENSE、迁移测试与 UI 冒烟测试。
- Release 签名改用 CI Secret，不再回退到 Debug 签名。

## 1.4.4
- **修复 Excel 导出空白文件问题**
  - 修正 Excel 写入顺序，先写入 workbook 再刷新输出流，避免系统文件选择器生成空白 xlsx 文件。
  - 导出写入器固定写入模板中的“使用说明”“测量记录”“用户资料”“导出信息”四个工作表，确保历史测量记录写入用户打开的正式工作表。
  - 表头写入改为复用现有单元格并覆盖标准列名，保持模板样式和导出列结构稳定。
  - 保留无历史记录时的诊断提示，避免生成看似成功但没有测量记录的备份文件。

## 1.4.3
- **修复 Excel 导出空白表格问题**
  - 修复导出时模板标题行被错误清除导致 Excel 表格空白的问题。
  - 新增保留模板标题行逻辑，确保导出格式正确。
  - 添加详细的导出诊断日志，便于问题排查。
  - 增强模板加载错误处理和提示。

## 1.4.2
- 设置页二级菜单优化。
- 数据管理页面重构。
- 导出功能界面改进。

## 1.1.4
- 优化趋势页双折线图：坐标轴、网格线、圆点节点、图例和卡片留白更清晰。
- 支持点击图表节点查看完整日期、时间、收缩压和舒张压。
- 选中节点增加高亮效果和竖向辅助线，提升查看体验。
- 支持目标血压参考线、30 天平均值展示和数据点横向滚动。
- 二级/三级页面返回按钮升级为圆形图标按钮，点击热区更舒适。

## 1.1.3
- 底部导航由 3 栏升级为 4 栏：测量 / 历史 / 趋势 / 设置。
- “血压趋势”从历史二级入口迁移为趋势一级页面。
- 历史页记录列表上移，减少层级跳转。
- 设置页二级/三级页面统一左上角返回按钮样式。
- 设置 -> 说明改为二级菜单页，新增“更新说明”和“测量建议”入口。
- 更新版本号为 `1.1.3` 并完成发布包构建。

## 1.1.2
- Stitch 风格界面完成增量落地，覆盖测量、历史、趋势、设置页面与关键弹窗状态。
- 保持现有 Room/DataStore/Repository/ViewModel 业务逻辑不变，仅优化 UI 层与交互呈现。
- 新增测量页支持“前 2 组固定 + 第 3 组起动态扩展”。
- 动态新增读数组参与平均值、分级和高风险判断。
- 趋势页升级为 7 天 / 30 天双折线时间序列图。
- 双折线图加入水平参考线，并明确标注收缩压/舒张压。
- 历史页保持列表主视图，支持分组浏览、详情查看、编辑与删除确认。
- 设置页保持一级菜单 + 二级详情页结构，数据管理能力持续可用。

## 1.1.1
- Stitch 风格界面增量落地。
- 历史趋势升级为 7 天 / 30 天双折线时间序列图。
- 图中加入血压参考线，并明确标注收缩压和舒张压。
- 保持 Room/DataStore/Repository 业务逻辑不变。
