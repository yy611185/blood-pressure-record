# 拍照扫描识别血压计读数 — 实施方案（v1.2，评审稿）

> 关联文档：《需求文档-拍照识别血压计（v1.1）》《实施方案-拍照识别血压计（v1.1）》
> 日期：2026-08-06　状态：**待用户确认，确认后施行**
> 本稿基于对现有代码库（blood-pressure-record v1.8.1，Compose + Room + DataStore）的实际核对后修订。

---

## 0. 结论摘要

- 现有实施方案（v1.1）总体方向正确：**CameraX 拍照 + ML Kit 离线 OCR + 行结构解析 + 按组确认保存 + 照片开关**，建议沿用其主干。
- 对照实际代码，v1.1 有 **6 处需要修正/澄清**（见 §3），其中 3 处直接影响能否通过验收：
  1. 现有 `MeasurementInputRules` 实际范围是 40–300 / 20–200 / 20–250，而 v1.1 写的是 60–280 / 40–180 / 30–220 —— 必须二选一；
  2. 现有 Repository 强制"一次记录至少 2 组读数"，而需求验收标准写"拍 1–3 张全流程可用" —— 1 张时需要小改保存链路；
  3. 现有数据模型"场景"只在记录（session）级，没有逐组场景 —— v1.1 建议的"逐组可改场景"实际成本很高，P0 应只做统一场景。
- 工作区/文档引用的实拍照片（`assets/bp-monitor-standby.jpg`、`bp-monitor-reading.jpg`）**在磁盘上不存在**；验收要求"维乐高实测 ≥90%（20 张样本）"依赖用户提供真机照片。

---

## 1. 需求要点回顾（依据需求文档 v1.1）

| 决策 | 结论 |
|---|---|
| D1 | 回显可编辑表单确认，无自动写入 |
| D2 | 拍 1 张 = 1 组，连续拍 2–3 张组成一次记录 |
| D3 | "保存识别照片"开关，默认关闭 |
| D4 | P0 先适配"三行堆叠"布局（维乐高实测） |
| D5 | 拍照识别 P0 优先于 PDF 导出 |

核心流程：拍照（1–3 张）→ 本机识别（无网络）→ 按组确认/编辑 → 复用现有 Repository 保存 → 保存后与手动录入数据一致。

---

## 2. 现有代码核对结论（关键事实）

| 事实 | 现状 |
|---|---|
| 架构 | Kotlin + Jetpack Compose（BOM 2024.10.00）+ Material3 + Navigation 2.8 + Room 2.6.1 + DataStore；minSdk 26 / compile & target 36；无版本目录，依赖直接写在 `app/build.gradle.kts` |
| 校验规则 | `MeasurementInputRules`：收缩压 **40..300**、舒张压 **20..200**、脉搏 **20..250**、舒张压<收缩压（需求文档 v1.1 写的 60–280/40–180/30–220 与代码不符） |
| 保存链路 | `BloodPressureRepository.saveSession(SaveSessionInput)` → `DefaultBloodPressureRepository` → `sessionDao.insertSessionWithReadings`（事务）。**`buildSessionEntity` 强制 readings 数量 2..20**，且校验未来时间 |
| 数据模型 | `measurement_sessions`（含 scene 记录级字段）+ `measurement_readings`（组数据，无 scene 字段）；Room 当前 v7，禁止 destructive migration |
| 多组手动录入 | `SessionFormLogic.validateAndBuildReadings(requiredCount = 2)`：手动录入至少 2 组 |
| 表单复用件 | `MeasurementReadingCard`、`MeasurementDateTimePicker`、`SessionSaveBottomBar`、`MeasurementTags`、`SessionFormLogic` 均可复用 |
| 设置链路 | `AppSettingsStore`(DataStore) + `SettingsRepository` + `SettingsViewModel`；数据管理页在 `SettingsDataManagementScreen`；开关样式可参考 `SettingsDisplayScreen`（`SettingsSwitchRow`） |
| 备份排除 | `backup_rules.xml` / `data_extraction_rules.xml` 已全量排除（root/file/database/sharedpref/external）→ 照片放 `filesDir` 自动受保护，无需改 |
| DI | `AppContainer`（DefaultAppContainer）+ `AppViewModelFactory` 精确类型匹配 |
| 导航 | `navigation/AppDestination.kt`（sealed class）+ `AppNavigation.kt` |
| 入口 | 测量页 = `DashboardScreen`，现有主按钮"记一次血压" |

---

## 3. 我的方案 vs 现有实施方案（对比）

| # | 对比项 | 实施方案 v1.1 | 我的方案（v1.2） | 结论 |
|---|---|---|---|---|
| 1 | 识别引擎 | ML Kit bundled 主通道 + 7 段兜底 + 自训 TFLite（二期） | 同方向；**P0 只做 ML Kit 主通道**，7 段兜底明确归 P1（v1.1 模块图里把兜底放 P0、里程碑又放 P1，自相矛盾） | 采纳我的方案 |
| 2 | ML Kit 模型 | 未区分模型 | **P0 用 Latin 模型**（`text-recognition`，体积小、数字足够）；若真机样本显示中文模型识别更佳再切 `text-recognition-chinese`；OCR 引擎做成接口可替换 | 采纳我的方案（v1.1 未写） |
| 3 | 相机 | camera-core/camera2/lifecycle/view + camera-compose | **用 camera-view 的 `PreviewView`**（AndroidView 嵌入 Compose），不引入尚不稳定的 camera-compose；音量键拍照通过 Activity `dispatchKeyEvent` → 共享事件流实现 | 采纳我的方案 |
| 4 | 校验范围 | 60–280 / 40–180 / 30–220 | **默认沿用现有 `MeasurementInputRules`（40–300 / 20–200 / 20–250）**；若确要收紧为需求文档数值，需同步改手动录入、导入与单测，作为独立决策 | 待用户确认（见 §5 Q2） |
| 5 | 单张保存 | 未处理"1 张"与 Repository 最少 2 组的冲突 | **`SaveSessionInput` 增加 `minReadings`（默认 2），扫描流程传 1**；手动录入行为不变，向后兼容 | 采纳我的方案（验收要求"拍 1–3 张可用"） |
| 6 | 逐组场景（O5） | "默认统一，可逐组改" | **P0 只做记录级统一场景**（与手动录入一致）；逐组场景需要 Room 迁移 + 备份格式升级 + 历史/编辑/导出联动，成本高，放 P2 且仅在确有需求时做 | 采纳我的方案 |
| 7 | 时间自动带出（O6） | P1 做 | **暂缓，用户确认先不做**（避免低可信时间写入记录） | 按用户确认 |
| 8 | 照片级联删除 | "删除记录时级联删除"（未写实现） | 新增 `ScanPhotoStore`；通过 Repository 删除回调 + `clearAllData` 钩子接线，纯文件删除，不碰 Room | 采纳我的方案（补充实现路径） |
| 9 | 照片保存时机 | 未明确 | **保存成功拿到 sessionId 后再写文件**（`filesDir/scan_photos/<sessionId>/<groupId>.jpg`）；取消/失败不落盘 | 采纳我的方案 |
| 10 | 解析器输入 | ML Kit `Text` 对象 | **先转成纯 Kotlin `OcrResult/OcrBlock`，解析器不依赖 Android/ML Kit**，满足"单测覆盖率 ≥90%" | 采纳我的方案 |
| 11 | 真机样本 | 验收要求 20 张，未说明来源 | 文档引用的实拍照片缺失 → **需要用户提供 ≥10–20 张维乐高实拍**（不同光照/角度/距离），开发期至少 5–10 张用于调参 | 待用户提供（见 §5 Q5） |

**结论：以实施方案 v1.1 为主干，按我的方案做上述 11 项修正后施行。**

---

## 4. 最终推荐实施方案（确认后施行）

### 4.1 技术选型与依赖

- 识别：`com.google.mlkit:text-recognition:16.0.1`（bundled Latin 模型，离线，不新增权限）
- 相机：CameraX `1.4.x`（camera-core / camera-camera2 / camera-lifecycle / camera-view）
- 预处理：灰度 + 对比度/锐化增强（`androidx.core:core-ktx` 已有），可选 Otsu 二值化用于兜底
- 权限：仅 `CAMERA`；Manifest 不得出现 `INTERNET`；加 `<uses-feature android:name="android.hardware.camera.any" android:required="false"/>`
- 体积：ML Kit Latin bundled 约 +5~8MB（release + minify 后），满足 NFR5（≤15MB）

### 4.2 文件清单

**新增（app/src/main/java/com/example/bloodpressurerecord/）**

```
domain/ocr/
  OcrModels.kt            # OcrBlock(text,left,top,right,bottom,confidence)、OcrResult —— 纯 Kotlin
  BpReadingParser.kt      # 行结构解析：OcrResult → ReadingCandidate —— 纯 Kotlin
  ReadingCandidate.kt     # ReadingCandidate(systolic,diastolic,pulse?,flags)
  OcrEngine.kt            # 接口：suspend fun recognize(bitmap): OcrResult
mlkit/MlKitOcrEngine.kt   # ML Kit 实现：Bitmap → InputImage → Text → OcrResult（Android 层适配）
data/scan/ScanPhotoStore.kt # filesDir/scan_photos/<sessionId>/<groupId>.jpg；save/delete/清理；纯 File 逻辑可单测
ui/scan/
  ScanCameraScreen.kt     # 取景/拍照/逐张识别结果卡片/继续拍/重拍/完成
  ScanReviewScreen.kt     # 按组确认页（复用 MeasurementReadingCard/DateTimePicker/SaveBottomBar）
  ScanViewModel.kt        # 流程状态机 + 暂存（内存）+ 保存
  ScanCameraUiModels.kt   # ScanGroup(thumbnail,candidate,flags)、ScanUiState
```

**修改**

| 文件 | 改动 |
|---|---|
| `app/build.gradle.kts` | +ML Kit、+CameraX 依赖 |
| `AndroidManifest.xml` | +CAMERA 权限、+camera uses-feature |
| `navigation/AppDestination.kt` | +`ScanCamera`、`ScanReview` 路由 |
| `navigation/AppNavigation.kt` | +两个 composable；Dashboard 入口导航 |
| `ui/home/DashboardScreen.kt` | "记一次血压"下方新增"📷 拍照识别"次按钮（并列入口） |
| `data/repository/BloodPressureRepository.kt` | `SaveSessionInput` + `minReadings: Int = 2` |
| `data/repository/DefaultBloodPressureRepository.kt` | `buildSessionEntity` 校验改用 `minReadings..MAX`；+`onSessionDeleted` 回调 |
| `data/datastore/AppPreferences.kt` | +`SAVE_SCAN_PHOTOS` 键 |
| `data/datastore/AppSettingsStore.kt` | +`saveScanPhotosEnabled`（默认 false）+ setter |
| `data/repository/SettingsRepository.kt` | +`setSaveScanPhotosEnabled`、+`cleanupScanPhotos(olderThanDays: Int?)` |
| `data/repository/DefaultSettingsRepository.kt` | 实现上述；`clearAllData` 同步清理照片目录 |
| `ui/settings/SettingsDataManagementScreen.kt` | +"保存识别照片"开关、+"清理识别照片"（全部/30 天前） |
| `ui/settings/SettingsViewModel.kt` | +对应状态与方法 |
| `AppContainer.kt` / `AppViewModelFactory.kt` | 注入 `ScanPhotoStore`、`OcrEngine`、`ScanViewModel` |

### 4.3 核心设计

**（1）状态机（ScanViewModel）**

```
Idle → 拍照 → Processing(单张) → 组结果暂存
  ├→ [继续拍] → 第 2/3 张 Processing
  ├→ [重拍当前组]
  └→ [完成] → Review(按组表单)
        → 确认保存 → Saving → Saved(返回测量页)
失败路径：Processing → "未识别到血压读数" → 重拍 / 手动输入（返回）
边界：最多 3 组；第 3 组拍完自动进入确认页
```

**（2）行结构解析（BpReadingParser，纯函数）**

```
输入 OcrResult（按 bounding box 归一化到图片尺寸）
1. 过滤：
   - 时间 \d{1,2}:\d{2} → 丢弃（P0；O6 在 P1 才用它）
   - 单个数字且框高远小于主数字（用户号"2"）→ 丢弃
   - 非数字块 → 丢弃（先剥离心形等符号，如 "❤69"→"69"）
2. 剩余数字块按 top 排序 → 按 Y 区间重叠聚类成行
3. 取框高最大的 2–3 行作为读数行：行1=收缩压、行2=舒张压、行3（若有）=脉搏
4. 校验复用 MeasurementInputRules（范围 + 舒张压<收缩压）
5. 输出 ReadingCandidate + flags：
   - AMBIGUOUS_DIGIT：OCR 置信度低 / 文本含易混模式
   - BOUNDARY：数值接近校验边界 ±10
   - PARTIAL_STRUCTURE：仅 2 行（脉搏可空，允许）
   - 1 行或 0 行 → 返回 null → UI 提示"未识别到血压读数"
```

**（3）确认页**

- 按组卡片：组序号 + 收缩压/舒张压/脉搏三输入框（复用 `MeasurementReadingCard`）+ 缩略图（点击放大核对）
- 可疑字段：黄色边框 + "请核对"角标（不阻止保存）
- 记录级：日期/时间（复用 `MeasurementDateTimePicker`，默认当前时间）+ 场景（复用 `MeasurementTags`，记录级统一）
- 保存：`SessionFormLogic.validateAndBuildReadings` 校验 → `repository.saveSession(input, minReadings=1)` → 成功后若有照片开关且开启 → `ScanPhotoStore` 落盘 → 返回测量页

**（4）照片处理（D3）**

- 开关：设置 → 数据管理 → "保存识别照片"，默认 **false**
- 关闭：照片仅内存，识别后释放全尺寸 Bitmap，只留缩略图供确认页核对，退出流程即释放
- 开启：保存成功拿到 sessionId 后写入 `filesDir/scan_photos/<sessionId>/<groupId>.jpg`（JPEG ~85%），不进入相册；已有备份规则全排除，无需改
- 清理：数据管理页"清理识别照片"（全部 / 30 天前）；删除记录（`deleteSession`）与"清除全部数据"（`clearAllData`）联动删除对应照片文件

**（5）入口与导航**

- Dashboard（测量页）"记一次血压"下方新增并列按钮"📷 拍照识别"
- 新路由：`scan/camera`（拍照）、`scan/review`（确认）；从确认页可返回相机页单独重拍某组
- 首次进入请求 CAMERA 权限；拒绝 → 提示并回退手动录入

**（6）音量键拍照**

- `MainActivity.dispatchKeyEvent` 捕获 `KEYCODE_VOLUME_UP/DOWN` → 写入共享 `MutableSharedFlow` → `ScanCameraScreen` 收集触发拍照；快门按钮与点按预览同样可拍

### 4.4 测试计划

| 层级 | 用例 |
|---|---|
| 单测（JVM） | `BpReadingParser`：三行/两行无脉搏/时间干扰/用户号干扰/心形符号/乱码/空输入/边界值/行聚类/置信度标记；`ScanViewModel`：1–3 组流程、重拍、取消、失败回退、保存成功/失败；`ScanPhotoStore`：save/delete/清理（临时目录）；Repository：`minReadings=1` 保存 1 组、默认仍拒绝 <2 |
| 仪器测试 | CAMERA 权限拒绝/授予路径；连拍流程注入假 Bitmap 全链路（compose ui-test） |
| 真机验收 | 维乐高实测 ≥20 张（不同光照/角度/距离）三行全对 ≥90%；待机界面正确提示；中端机单张 ≤3s |

### 4.5 里程碑

| 阶段 | 内容 | 交付 |
|---|---|---|
| P0 MVP | 入口+权限、CameraX 连拍 1–3、ML Kit OCR、行结构解析、按组确认保存（复用 Repository）、照片开关+存储+清理、单测 | 可用 APK；真机样本 ≥90% |
| P1 增强 | 模糊/过曝质量提示、7 段数码管兜底算法、通用单行 `128/85` 布局（O6 时间带出暂缓） | 更新包 |
| P2 联动 | PDF 报告原始证据引用、自训 TFLite 数字模型压缩体积、（可选）逐组场景 | 优化包 |

### 4.6 风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| 7 段 LCD 对 ML Kit 识别率不确定（无样本可先验证） | 中 | 预处理增强 + 解析器兜底到"手动输入"；P1 上 7 段专用算法；**先提供 5–10 张样本快速试跑** |
| 反光/倾斜/模糊 | 中 | 取景引导框 + 重拍提示 + 人工确认兜底 |
| 1 组保存改动影响面 | 低 | `minReadings` 默认 2，仅扫描路径传 1；补回归测试 |
| ML Kit bundled 体积 | 低 | +5~8MB，满足 NFR5；P2 自训模型再压缩 |
| 连拍内存 | 低 | 逐张释放全尺寸 Bitmap，仅留缩略图 |
| Room/备份格式 | 无 | P0 不改 schema、不改备份格式（逐组场景不做） |

---

## 5. 待确认事项

1. **单张保存**：确认允许"只拍 1 张 = 1 组也可保存"？（推荐：允许，Repository 加 `minReadings`，默认手动录入仍 2 组）
2. **校验范围**：沿用现有 `MeasurementInputRules`（40–300/20–200/20–250，推荐，零影响）；还是按需求文档收紧为 60–280/40–180/30–220（需同步改手动录入、导入与测试）？
3. **逐组场景（O5）**：P0 只做记录级统一场景？（推荐；逐组场景成本高，留 P2）
4. **屏幕时间带出（O6）**：P1 再做？（推荐）
5. **真机样本**：请提供维乐高血压计实拍照片（建议 ≥10–20 张，含待机界面、不同光照/角度/距离），用于开发期调参与最终验收；当前磁盘上找不到文档引用的 `assets/bp-monitor-*.jpg`。
6. **照片开关位置**：放"设置 → 数据管理"页（推荐，与清理入口同页）。

确认以上 6 项（或仅回复"按推荐执行"）后，即可开始 P0 开发。
---

## 6. 用户确认记录（2026-08-06）

| # | 决策点 | 用户结论 |
|---|---|---|
| 1 | 单张保存 | 允许：扫描流程 1 组也可保存（Repository 加 `minReadings`，默认 2，手动录入不变） |
| 2 | 校验范围 | 沿用现有 `MeasurementInputRules`（40–300 / 20–200 / 20–250） |
| 3 | 逐组场景（O5） | P0 只做记录级统一场景 |
| 4 | 时间带出（O6） | 先不做（暂缓） |
| 5 | 真机样本 | 已建 `scan_samples/` 文件夹，用户稍后提供实拍照片 |
| 6 | 照片开关位置 | "设置 → 数据管理"页 |

**状态：方案已确认，P0 开发开始。**